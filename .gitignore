# Node.js dependencies (installed automatically in Amplify)
node_modules/

# Build output (Amplify generates this during deployment)
dist/

# Local environment variables (Amplify injects env variables)
.env

# Cache files (improve local performance but not needed in Git)
.vite/
.vscode/
.DS_Store
Thumbs.db

# AWS Amplify local config (Amplify manages this remotely)
amplify/.config/local-*

# Optional: Lockfiles (keep only the one your package manager uses)
yarn.lock
pnpm-lock.yaml